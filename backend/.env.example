# Server
PORT=3001

# Database (matches docker-compose.yml)
DATABASE_URL="postgresql://concert:concert@localhost:5432/concert_db?schema=public"

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d
